# Cookify — React frontend

See the top-level `../README.md` for full project context, setup, and why this exists alongside the original `../frontend/` (plain HTML/CSS/JS) implementation.

```bash
npm install
npm run build      # outputs to dist/, served by the backend
npm run dev        # local dev server with HMR, if iterating on this alone
```
